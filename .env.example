# Frontend Environment Variables
# Copy this file to .env.local and fill in the values

# Supabase Configuration (Required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: Enable debug logging
# VITE_DEBUG=true

# Supabase Edge Function Secrets
# These are set in Supabase Dashboard -> Edge Functions -> Secrets

# n8n Webhook URLs (Required)
# NOTEBOOK_CHAT_URL=https://your-n8n-instance.com/webhook/notebook-chat
# NOTEBOOK_GENERATION_URL=https://your-n8n-instance.com/webhook/notebook-generation
# AUDIO_GENERATION_WEBHOOK_URL=https://your-n8n-instance.com/webhook/audio-generation
# DOCUMENT_PROCESSING_WEBHOOK_URL=https://your-n8n-instance.com/webhook/document-processing
# ADDITIONAL_SOURCES_WEBHOOK_URL=https://your-n8n-instance.com/webhook/additional-sources

# Authentication for n8n webhooks (Required)
# NOTEBOOK_GENERATION_AUTH=Bearer your-webhook-auth-token

# OpenAI Configuration (Required for AI features)
# OPENAI_API_KEY=sk-your-openai-api-key

# Admin Setup
# After deployment, set the first admin user by running this SQL:
# UPDATE profiles SET role = 'admin' WHERE email = 'admin@example.com';

# Permission-Based Features
# The admin-controlled version uses tags and permissions to control access
# No additional environment variables needed - all managed through the UI